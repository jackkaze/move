<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteBase /
    RewriteCond $1 !^(index\.php|data|images|swf|uploads|js|css|assets|robots\.txt|$)
    RewriteRule ^(.*)$ /index.php/$1 [L,QSA]
</IfModule>